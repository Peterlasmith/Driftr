// GENERATED FILE (via `npm run build:extension`)
// This placeholder exists so the extension loads in dev checkouts.
// Run the build script to bundle the Firebase SDK for MV3.

function notBuilt() {
  throw new Error(
    "Firebase bundle missing. Run `npm install` then `npm run build:extension` to generate extension/vendor/firebase.js"
  );
}

export function initializeApp() {
  return notBuilt();
}

export function getFirestore() {
  return notBuilt();
}

export function collection() {
  return notBuilt();
}

export function addDoc() {
  return notBuilt();
}

